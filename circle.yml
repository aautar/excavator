machine:
  php:
    version: 7.0.4

test:
  override:
    - vendor/bin/phpunit --coverage-clover=coverage.xml --bootstrap vendor/autoload.php tests
  post:
    - bash <(curl -s https://codecov.io/bash)
